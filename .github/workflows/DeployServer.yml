name: Deploy Server

on:
  push:
    branches:
      - master
    paths:
      - "src/*"
      - ".github/workflows/DeployServer.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: test workflow
        run: echo "YAY this workflow is running"
      - name: Add github username
        run: git config --global user.name 'atharav443'
      - name: Add github email
        run: git config --global user.email 'atharavnaik3@gmail.com'
      - name: add heroku remote
        run: git remote add heroku https://heroku:685df7e1-ff0e-4538-a4ad-21861fe3cfac@git.heroku.com/atharav-recipe-api.git
      - name: Fetch repo
        run: git fetch --unshallow
      - name: push to heroku remote
        run: git push heroku master --force
